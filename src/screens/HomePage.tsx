// libs
import React from 'react';
import {SafeAreaView, ScrollView, View} from 'react-native';

const HomePage = () => {
  return (
    <>
      <SafeAreaView>
        <ScrollView>
          <View />
        </ScrollView>
      </SafeAreaView>
    </>
  );
};

export default HomePage;
